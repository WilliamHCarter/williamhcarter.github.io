<div class="hero-section flex mt-20 w-full">
  <!-- Blurred Background Image -->
  <div class="bg-blur backdrop-blur-xl bg-[ffffff60]"></div>

  <!-- Floating Orbs -->
  <div class="orb orb1"></div>
  <div class="orb orb2"></div>

  <!-- Content -->
  <div class="z-[3] flex flex-col items-center justify-center h-full w-full animate-intro-100 mt-5 delay-def">
    <h2 class="text-5xl font-semibold delay-def">Will Carter</h2>
    <p class="text-xl mt-1" >Developer | Designer | Creative</p>
  </div>
</div>

<style>

  /* Basic styles for the hero section */
.hero-section {
  position: relative;
  height: 500px; /* Adjust this to your desired height */
  overflow: hidden;
}

/* Blurred Background */
.bg-blur {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 2;
}

/* Floating Orbs */
.orb {
  position: absolute;
  width: 400px; /* Adjust this for orb size */
  height: 400px;
  border-radius: 50%;
  z-index: 1;
}

.orb1 {
    top: 10%;
    left: -10%;
    background-color: rgb(82, 70, 255);
    animation: drift1 42s infinite cubic-bezier(0.42, 0, 0.58, 1);
  }

  .orb2 {
    top: 30%;
    left: 70%;
    background-color: rgb(62, 226, 196);
    animation: drift2 12s infinite alternate;
  }

  @keyframes drift1 {
    0% {
        transform: translate(0, 0); /* Starting position */
    }
    33% {
        transform: translate(200px, 150px); /* Middle position */
    }
    66% {
        transform: translate(400px, 0px); /* End position (back to start) */
    }
    100% {
        transform: translate(0, 0); /* Starting position */
    }
  }

  @keyframes drift2 {
    from {
      transform: translate(0, 0);
    }
    to {
      transform: translate(-150px, 200px); /* Adjust as needed */
    }
  }

</style>
<div class="hero-section mt-20 w-full">
  <!-- Blurred Background Image -->
  <div class="bg-blur backdrop-blur-xl bg-[ffffff60]"></div>

  <!-- Floating Orbs -->
  <div class="orb orb1"></div>
  <div class="orb orb2"></div>

  <!-- Content -->
  <div class="flex flex-col items-center justify-center h-full w-full animate-intro-100 mt-5 delay-def">
    <h2 class="text-5xl font-semibold delay-def">Will Carter</h2>
    <p class="text-xl mt-1" >Developer | Designer | Creative</p>
  </div>
</div>

<style>

  /* Basic styles for the hero section */
.hero-section {
  position: relative;
  height: 500px; /* Adjust this to your desired height */
  overflow: hidden;
}

/* Blurred Background */
.bg-blur {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 2;
}

/* Floating Orbs */
.orb {
  position: sticky;
  width: 400px; /* Adjust this for orb size */
  height: 400px;
  border-radius: 50%;
  z-index: 1;
}

.orb1 {
  top: 10%;
  left: -10%;
  background-color: rgb(82, 70, 255); /* Semi-transparent white. Adjust as needed */
  animation: drift 10s infinite alternate;
}

.orb2 {
  top: 30%;
  left: 70%;
  background-color: rgb(62, 226, 196); /* Semi-transparent white. Adjust as needed */
  animation: drift 12s infinite alternate;
}

@keyframes drift {
  from {
    transform: translateY(0);
  }
  to {
    transform: translateY(30px); /* Adjust this to change the drift distance */
  }
}

/* Content Style */
.content {
  position: relative;
  z-index: 3;
  text-align: center;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

</style>